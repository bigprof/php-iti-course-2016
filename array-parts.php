<?php
	$codes = [
		'html' => 'HyperText Markeup Language',
		'ftp' => 'File Transfer Protocol',
		'http' => 'HyperText Transfer Protocol'
	];
	
	var_dump(array_keys($codes));
	
	var_dump(array_values($codes));
	
	var_dump($codes);